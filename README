Tiny Issue Tracker
------------------

Tiny Issue Tracker (TIT) is PHP/SQLite based, one-file issue tracker. 

Features:
 - Super Lightweight
 - Multi-user
 - Auto DB creation on first run
 - Issue Priority (High, Medium, Low)
 - Throw and run!

Installation:
 - Modify configuration in the file (tit.php)
 - Upload the file to webserver (rename if necessary)
 - Load file from browser 
   (DB will be created automatically on first run)